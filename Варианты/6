# Список русских гласных букв для быстрой проверки.
# Вынесен за пределы функции, чтобы не создавать его каждый раз.
RUSSIAN_VOWELS = {'а', 'е', 'ё', 'и', 'о', 'у', 'ы', 'э', 'ю', 'я'}

def count_vowels_in_string(s):
    # Базовый случай: если строка пуста, гласных нет.
    if not s:
        return 0
    
    # Рекурсивный шаг:
    # 1. Проверяем, является ли первый символ гласной (делаем его маленькой буквой для сравнения).
    #    Если да, добавляем 1 к счетчику, иначе 0.
    # 2. Прибавляем это к результату подсчета гласных в оставшейся части строки (все, кроме первого символа).
    return (1 if s[0].lower() in RUSSIAN_VOWELS else 0) + \
           count_vowels_in_string(s[1:])

# Примеры использования:
print(f"В 'Привет мир!' {count_vowels_in_string('Привет мир!')} гласных")
print(f"В 'Один Два Три' {count_vowels_in_string('Один Два Три')} гласных")
print(f"В 'Тест' {count_vowels_in_string('Тест')} гласных")
print(f"В '' (пустой строке) {count_vowels_in_string('')} гласных")
print(f"В 'РТГ' {count_vowels_in_string('РТГ')} гласных")
print(f"В 'АБВГДЕЖЗИКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯ' {count_vowels_in_string('АБВГДЕЖЗИКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯ')} гласных")
 
