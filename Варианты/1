def sum_digits(n):
    # Если число однозначное (от 0 до 9), возвращаем его.
    if n < 10:
        return n
    # Иначе складываем последнюю цифру с суммой цифр оставшегося числа.
    else:
        return (n % 10) + sum_digits(n // 10)

print(sum_digits(123))  # Выведет 6



